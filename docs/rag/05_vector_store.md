## 4. 벡터 스토어 (Vector Store)

### 정의
벡터 스토어는 **RAG 시스템의 4번째 단계**로, 임베딩된 벡터를 저장하고 검색하는 데이터베이스입니다.

```
문서 로드 → 텍스트 분할 → 임베딩 → 벡터 스토어 저장
```

### 핵심 기능

| 기능 | 설명 |
|:---|:---|
| **저장 & 색인화** | 임베딩 벡터를 효과적으로 저장하고 인덱싱 |
| **빠른 검색** | 대량 데이터에서 관련 정보를 빠르게 조회 |
| **확장성** | 데이터 증가에도 성능 저하 없이 관리 가능 |
| **의미 검색** | 질문과 의미적으로 유사한 청크 조회 |

### 로컬 vs 클라우드

| 구분 | 로컬 (Local) | 클라우드 (Cloud) |
|:---|:---|:---|
| **비용** | 무료/저렴 | 사용료 발생 |
| **보안** | 데이터 외부 유출 X | 클라우드 의존 |
| **확장성** | 문서 증가 시 속도 저하 | 대규모 최적화 |
| **설치** | 직접 설정 필요 | 간편한 연동 |

### 주요 벡터 스토어 비교

| 이름 | 타입 | 특징 |
|:---|:---|:---|
| **Chroma** | 로컬 | 가볍고 빠름, 개발/테스트용 추천 |
| **FAISS** | 로컬 | Meta 개발, 대규모 검색 최적화 |
| **Pinecone** | 클라우드 | 관리형 서비스, 확장성 뛰어남 |
| **Weaviate** | 클라우드/로컬 | 하이브리드 검색 지원 |
| **Qdrant** | 클라우드/로컬 | 고성능, 필터링 강점 |

> **LangChain에 통합된 벡터 스토어만 **50가지 이상**

### 코드 예시 (Chroma)

```python
from langchain_community.vectorstores import Chroma
from langchain_community.embeddings import HuggingFaceEmbeddings

# 임베딩 모델
embeddings = HuggingFaceEmbeddings(model_name="jhgan/ko-sroberta-multitask")

# 벡터 스토어 생성 및 저장
vectorstore = Chroma.from_documents(
    documents=chunks,           # 청크 리스트
    embedding=embeddings,       # 임베딩 모델
    persist_directory="./chroma_db"  # 저장 경로
)

# 검색 (유사도 기반)
results = vectorstore.similarity_search("슬개골 탈구 보장", k=5)
```

---

## 

```
[청크] ──임베딩──▶ [벡터] ──저장──▶ 🗄️ 벡터 스토어
                                         │
[질문] ──임베딩──▶ [벡터] ──검색──────────┘
                                         │
                               ▼ 유사도 높은 청크 반환
```

### 선택 관련 내용

| 상황 | 추천 |
|:---|:---|
| 개발/테스트 | Chroma (로컬, 간편) |
| 대규모 프로덕션 | Pinecone, Qdrant (클라우드) |
| 보안 중요 | FAISS, Chroma (로컬) |
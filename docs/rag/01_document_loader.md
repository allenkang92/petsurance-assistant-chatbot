## 1. 문서 로더 (Document Loader)

### 정의
문서 로더는 다양한 파일 형식으로 데이터를 읽어들이고, 이를 **추출 및 변환**하여 처리 가능한 상태로 만드는 도구입니다.  
특정한 데이터 구조나 요구 사항에 맞게 커스터마이즈하여 활용할 수 있습니다.

### 문서 로더의 역할
*   **RAG 시스템의 첫 번째 단계**: 우리가 가진 문서(Document)를 데이터베이스에 불러오는 역할을 담당합니다.

### Document 객체

| 속성 | 설명 |
|:---|:---|
| `page_content` | 문서의 **텍스트 내용**이 저장됩니다. |
| `metadata` | 문서와 관련된 **속성값** (예: 파일명, 출처, 페이지 번호 등)을 담습니다. |

> **💡 핵심 포인트**  
> 파이썬으로 문서 로더를 사용하거나 자신만의 로더를 직접 구현하려면, 반드시 문서를 `Document` 객체로 감싸줘야 LangChain과 호환됩니다.

### 코드 예시

```python
from langchain.schema import Document

# Document 객체 직접 생성
doc = Document(
    page_content="이것은 펫보험 약관의 내용입니다.",
    metadata={"source": "petsurance/약관.md", "page": 1}
)

print(doc.page_content)  # 텍스트 내용
print(doc.metadata)      # {'source': 'petsurance/약관.md', 'page': 1}
```